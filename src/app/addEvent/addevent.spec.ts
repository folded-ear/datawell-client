import { Component } from '@angular/core';
import {
    beforeEachProviders,
    describe,
    inject,
    injectAsync,
    it
} from '@angular/core/testing';

describe('AddEvent', () => {

    it('true is true', () => expect(true).toEqual(true));

});
